import React from "react";

import Page from "./page";

const HomePage = () => (
	<Page>
		<h1>Welcome to my React Server side rendered website</h1>
	</Page>
);

export default {
	component: HomePage
};
